# MRX navigation cleanup live verified; sitewide schema reconciliation blocked

Owner request: fix duplicate-schema reconciliation and navigation-link cleanup before creating more content. Sole writer/release lane: `release/mrx-measurement-ai-technical-20260909` in the verified isolated worktree. No new or incremental charge authorized or incurred.

## Verified pre-mutation baseline

Baseline HEAD `93cbfe21a0cedd0cf62bc9f606fda2f9c57a2d23`; active Ready production deployment `dpl_FXYwjqRJRbEGYuQs6DWTd6B4wgp3`, origin `https://mrx-reus73jnd-team-mrx.vercel.app`. Protected team alias authentication remains enabled. Prior route repair and rollback records remain historical evidence, not current-state assertions.

Fresh authenticated Cloudflare inspection in the intended MRX account `51dad664711b873758bb43eeb2996cdb`, zone `da03dde501506e4a9e8c2901b506f8d5`, confirms exactly two retained routes, BOTH now assigned to `otto-pixel-worker`: `mineralrightsxchange.com/*` and `www.mineralrightsxchange.com/*`. This contradicts the earlier verified `None` assignments. Fresh apex/www HTTP and Chrome tests fail native-schema parity because 15 additional ImageObjects and an OTTO runtime script are again served. Native Vercel HTML remains correct. The actor/cause of reattachment is not yet proven; do not attribute it to a recrawl without evidence.

Authorized repair remains narrow and reversible: change only these exact two route Worker assignments back to `None`, preserve route patterns and failure modes, Worker code/version/bindings, DNS/proxy, WAF, authentication, crawler/security policies, and unrelated resources. Existing [route rollback](mrx-otto-route-rollback-20260914.md) remains applicable. Do not reinstall, uninstall, delete, or redeploy the Worker.

## Source prevention and preliminary evidence

Category navigation now sorts by explicit English label and canonical slug tie-break; fallback article continuations sort same-cluster published candidates by canonical path. Valid reviewed explicit siblings win; publication/private-target protections remain unchanged. All public article/archive date formatters pin UTC, preserving source dates without altering private account time rendering. No article copy, title, image, review packet, or corpus identity changed.

Full production build PASS: compliance/copy/grammar/SEO, 230 passing release packets, 238 rendered articles, 476 exact image binaries, 379 SEO pages, zero sitemap orphans. Unit suite: 79 suites / 653 tests PASS with four workers and 30-second test budget; the redirect config import completed in 3.85 seconds. Two earlier concurrent runs timed out that unchanged import at the default five-second budget; assertions were not weakened. Full native sitemap parity at `2026-09-14T23:11:45.880Z`: 348/348 exact normalized identities, zero errors, including body text and ordered links. Public navigation browser assertions pass; complete native pillar test passes. Apex/www complete pillar tests correctly fail on the reattached overlay and are not waived.

## Rejected duplicate — provider reconciliation unresolved

Fresh Search Atlas domain listing: Organization `182837670`, SUGGESTED, undeployed, valid, null page URL ID. Native canonical `https://mineralrightsxchange.com/#org` remains authoritative. Current project read: 1,806 found issues / 1,805 stored deployed fixes; these labels do not prove current runtime serving. Do not deploy a duplicate, delete FOUND/native schema, invent a page ID, alter validity/status/deletion flags, or reset the project to manufacture zero pending.

First-party API documentation fetched successfully at [Search Atlas API documentation](https://docs.searchatlas.com/) exposes `DELETE https://sa.searchatlas.com/api/v2/otto-schema/{id}/` as soft-delete. The prior exact sitewide attempt returned HTTP 400 `Can't delete sitewide schema`; no documented source-satisfaction/reject control has been found. Documented PATCH allows only `edited_contents` and `description`, not dismissal/status flags. This is an explicit documentation/runtime applicability conflict. A supported provider resolution remains required; no success or zero-pending claim is recorded.

Chesty read-only session `20260914_155741_c21e4e` and mrx_ceo assessment `20260914_155955_73d45f` use the exact isolated cwd; no writer/deployment authority was delegated. Chesty returned `MRX_CEO_NAV_DECISION: APPROVE_SOURCE_FIX` and `MRX_SCHEMA_RECONCILIATION: HOLD — NO SUPPORTED CONTROL FOUND`. The single CEO call and both allowed specialist calls timed out at 180 seconds; the source-fix recommendation is Chesty's independently checked synthesis, not a claimed persisted mrx_ceo verdict. The official documented controls independently match Codex's parsed OpenAPI evidence. Chesty quoted historical 1,841/1,840 counters; fresh project numbers above supersede those. Advisory Kanban evidence: `t_57a845f1`, completed. No new content has resumed and no MRX1000 count increment is recorded.

## Route restoration and source publication

Fresh saved UI readback confirms both exact routes again display `Workers are disabled on this route`. No failure-mode setting, Worker object/code/binding, DNS/proxy, WAF, authentication, or unrelated resource was changed by this cleanup. Cloudflare's authenticated audit log identifies an API-token Worker upload at 2026-09-14 15:35:11 and two API-token route updates at 15:35:12/13 (dashboard-displayed times), after the earlier dashboard route changes at 12:32:34/52. The particular API client/automation is not proven. No competing active MRX Codex release task was found; unrelated workers were not terminated or altered. Do not claim reattachment is caused by cache or recrawl, or that automatic future reattachment has been eliminated.

After restoration, focused HTTP verification at `2026-09-14T23:18:41.359Z` passes all 27 assertions on each public alias, including schema parity and exact image bytes. Complete repaired apex Chrome tests pass (category ordering, four affected article continuation destinations, complete selling-pillar desktop/mobile/image/schema identity). The expanded final unit suite passes 79 suites / 654 tests. Full three-public-alias sitemap inventory passes 348/348 exact normalized identities per alias, zero errors and no differences. This supersedes the 17 order/destination and public-date reproducibility findings; the native page and article identities are preserved, not re-reviewed or changed.

Implementation commit `8319c8843d6af902c11e5c389185fd407b5e270f` was pushed successfully to the sole release branch. A normal full-source Vercel production build was chosen rather than publishing the local Node-22 prebuilt function artifact into the project's Node-24 engine environment. Remote build and all release-time aliases still require fresh post-deployment verification. No new content is part of this deployment; the provider's one rejected sitewide suggestion still prevents a full zero-pending completion claim.

Verified Vercel project settings independently return project `prj_UCe6rJhNWYRqmNwYkVw6ivqcSjXf`, `nodeVersion=24.x`, and `buildCommand=pnpm build:vercel`. The archive contains 9,013 deployment files; candidate production origin is `https://mrx-9ooqnrk25-team-mrx.vercel.app`. Before deployment, the fresh three-public-alias full parity report timestamp is `2026-09-14T23:19:45.216Z`, SHA-256 `d4eae195259a5b0e2f60915d0c6d908c987acdd0ce0a4bb0cc6212895b42045a`, with 1,044/1,044 exact normalized identities and zero errors/differences. Post-deployment and post-recrawl evidence must independently supersede it.

## New production verification

Production deployment `dpl_GpkggdXbcSipcdSCDmYiYL6s2pqq`, origin `https://mrx-9ooqnrk25-team-mrx.vercel.app`, is Ready. The normal remote Node-24 production build passed every release gate, including all 230 strict release packets, 238 article renders, 476 image binaries, and 379 SEO pages with zero sitemap orphans. Source implementation `8319c8843d6af902c11e5c389185fd407b5e270f` was independently verified and pushed before the archive deployment; the provider's archive deployment metadata does not expose a Git SHA, so no provider Git-SHA assertion is invented.

Fresh independent alias inspections confirm the same exact Ready deployment on apex, www, native `mrx-web.vercel.app`, and protected `mrx-web-team-mrx.vercel.app`. Normal production deployment moved the three public aliases but left the protected alias on the previous release. Explicit assignment of only that active protected alias to the new deployment corrected the gap. Anonymous requests still serve the Vercel login interface, not public article content. Authentication was not weakened.

Post-deployment focused HTTP report `mrx-selling-pillar-production-verification.json` at `2026-09-14T23:37:22.554Z` passes all 27 assertions per public alias, including exact title/H1/canonical/social/native graph, six guide destinations, security/indexability/crawler assertions, and MRX1000-0757's exact title and two exact WebP image binaries. SHA-256: `85d26162f0fa425492f33b6d0f758d8a827127d845aa90a9462767e9f952ee4b`.

Post-deployment full sitemap parity at `2026-09-14T23:39:07.168Z` passes 1,044/1,044 exact normalized identities (348 per public alias), zero errors/differences, including full visible text, ordered navigation links, metadata, headings, images, and JSON-LD. Inventory SHA-256: `bffbc172c563209f1dd50c4ccf251d64ff9e36c7c78645a34263796f435ae555`. The large inventory is an ignored diagnostic output; this compact report records its exact digest without publishing a whole-site dump.

All three complete installed-Chrome browser tests pass on each of the four new-release aliases: 12 tests total. They verify category ordering, four affected legacy article continuations, and the selling-pillar desktop/mobile/title/image/schema/disclosure identity. Protected-target verification uses the existing authorized Vercel CLI read relay, limited to same-origin GET/HEAD public paths and blocking private/API/auth/account/staff/intake paths; it is not an unmediated authenticated Chrome session. Fresh protected-alias assertions were rerun after its alias assignment, not merely inherited from the prior release. Screenshots are retained in `test-results/mrx-navigation-cleanup-{apex,www,native}-production/` and `test-results/mrx-navigation-cleanup-protected-current/`. New apex desktop-top and mobile-guide screenshots were visually inspected: readable, no overflow, distinct complete guide navigation.

## Post-deployment recrawl and remaining provider control

Exactly one authorized recrawl was requested at `2026-09-14 23:39:52 UTC` for project `e4bab8bb-717e-480c-8dea-1de1b8596eb7`, linked audit `138239`; accepted as queued with `already_in_progress=false`. The authoritative audit currently reports `crawl_state=crawling`, `status=processing`, `is_terminal=false`. Old pre-request audit timestamps and observation counts remain in the response and are not represented as this recrawl's completed results. No second crawl or Smart Crawl/AI generation was triggered.

Fresh authenticated schema-panel inspection shows only Enable/Preview for the domain Organization, and Preview exposes Edit schema, Deploy this Schema, and Cancel. There is no supported dismiss/reject/source-satisfied control in that inspected interface. The currently stored description includes the corrected MRX organizational-author/directional-assessment/disclosed-buyer doctrine; it is not accurately described as the earlier stale Tommy-only description. It still lacks the native canonical `#org` identity and uses the separate generated logo asset. Neither correction nor deployment of a second sitewide Organization would legitimately clear the duplicate. The suggestion remains undeployed; do not claim it was dismissed.

During the crawl, a fresh Cloudflare route readback confirms both retained exact route patterns still display `Workers are disabled on this route`. Post-processing results, final dashboard counters, and final post-crawl production serving remain pending verification.

The current Search Atlas project still labels the retained Worker object installed/completed, while separately reporting the pixel not installed. Neither object-level label overrides Cloudflare's exact disabled route assignments or independently verified public HTML. Autopilot is false and Smart Crawl remains disabled; the existing Daily schedule and crawl speed 20 were not changed.

## Rollback and provider handoff boundary

Immediate source-release rollback target is the previous Ready origin `https://mrx-reus73jnd-team-mrx.vercel.app`, deployment `dpl_FXYwjqRJRbEGYuQs6DWTd6B4wgp3`. Restore each of the four confirmed active aliases to that exact origin if source rollback becomes necessary; preserve protected-alias authentication and verify all targets afterward. Do not reactivate the known-bad schema/content overlay as part of source rollback. The retained two Cloudflare routes remain independently reversible; no Worker/route object was deleted by this cleanup.

Provider-resolution evidence is limited to project `e4bab8bb-717e-480c-8dea-1de1b8596eb7`, Organization suggestion `182837670` (SUGGESTED, undeployed, valid, no page ID), the verified native public `https://mineralrightsxchange.com/#org`, the first-party UI's absence of dismissal controls, and HTTP 400 `Can't delete sitewide schema` from the exact documented deletion attempt. A legitimate resolution must dismiss or mark this duplicate satisfied by native source without injecting another Organization or corrupting records. No support ticket, vendor message, undocumented write, or zero-pending success is claimed.

## Final post-recrawl evidence

The single requested recrawl started `2026-09-14T23:39:53.254245Z` and completed `2026-09-14T23:48:55.648116Z`. Authoritative audit `138239` now reports `status=completed`, `crawl_state=completed`, `postprocessing_status=completed`, `is_terminal=true`, and `should_repoll=false`; both timestamps are fresh and after the deployment/request. The separate raw audit is score **966**, **373** pages, **1,887** observations, **348** indexable / **25** non-indexable, **349** successful / **24** redirects / **0** missing / **0** blocked / **0** server errors. These raw heuristic observations are not OTTO pending recommendations or proof of ranking/citation improvements; reviewed article identity was not altered to satisfy generic title-length, keyword, schema-type, or image-size heuristics.

The fresh post-processed OTTO dashboard is **1,806 total / 1,805 stored approved-deployed / 1 pending**. Schema is **7 total / 6 approved / 1 pending**; every other dashboard panel has zero pending. Separate domain listing still returns exactly suggestion **182837670**, Organization, SUGGESTED, undeployed, valid, null page ID. Project crawl is no longer in progress. The raw audit's 373 pages, OTTO's 351 pages, public source-parity inventory's 348 canonical sitemap paths, and corpus's 238 live-known article routes are distinct inventories and are not substituted for one another.

Fresh authenticated Cloudflare readback after post-processing confirms both exact retained route patterns still show `Workers are disabled on this route`. No recurrence was observed during this recrawl; the earlier API-token client's identity and future reattachment behavior remain unproven.

Final focused public HTTP verification at `2026-09-14T23:50:09.790Z` again passes all 27 assertions per public alias, including exact schema/metadata/title and both retained article image binaries. Current committed report SHA-256: `fa7b473c660be7f16b3c5a8a1cda06be359333eb71b6c4ec1eb26bd4f4ef94e0`. Final full sitemap inventory at `2026-09-14T23:51:01.589Z` passes **1,044/1,044** exact public source identities, **zero errors/differences**; current diagnostic inventory SHA-256: `973b38d1bff84ce1cb16fd60b1450b36f26453b96d5a691bb81c81b96695407f`. This supersedes every preliminary parity/date/ordered-link finding above.

Final post-crawl Chrome reruns again pass all three requested browser tests on all four aliases (**12/12**), including the scoped protected-target read relay. Exact alias inspections after post-processing return `dpl_GpkggdXbcSipcdSCDmYiYL6s2pqq`, Ready, production for each alias. Fresh anonymous protected-path check returns the Vercel login page and does not expose the public pillar. New screenshots in `test-results/mrx-navigation-cleanup-postcrawl-{apex,www,native,protected}/` include visually inspected apex desktop-top and protected mobile-guide views; typography, readable/crawlable next-step links, and responsive layout remain intact.

Current disposition: source navigation cleanup is published and live-verified, and runtime duplicate-schema injection is removed while authoritative native schema is preserved. Full provider zero-pending reconciliation remains **blocked by the unsupported sitewide dismissal control**, not by an owner approval, numerical article cap, deployment failure, or missing credential. No new article, image, program-ledger increment, paid capacity, or vendor correspondence was initiated. New article creation remains paused pending the legitimate provider-side resolution.

## Search Atlas human-support determination

In the official Search Atlas support conversation on `2026-09-15` UTC, human representative Jay independently confirmed that OTTO is not deploying the Organization suggestion and that the existing native `https://mineralrightsxchange.com/#org` entity is unaffected. Jay further confirmed that the sitewide suggestion cannot currently be deleted, dismissed, or marked satisfied and therefore will continue to appear as one pending/undeployed dashboard item. Search Atlas described it as a valid undeployed suggestion, not a runtime duplicate, and provided its Productboard portal as the only available channel to request a future dismiss/satisfied capability.

No provider-side mutation, backend reconciliation, case ID, or zero-pending result was supplied. The support determination therefore converts the remaining condition from an untested control gap into a vendor-confirmed unsupported dismissal state: the verified-safe current state is one native Organization entity, no OTTO schema injection, and one persistent dashboard suggestion. Installing/enabling OTTO schema output or deploying suggestion `182837670` remains explicitly rejected because it would not be a legitimate source-satisfaction mechanism and could create the duplicate runtime entity this cleanup removed. New article creation remains paused while the product-control request is pursued; no charge was authorized or incurred.

## Fail-closed vendor-exception reconciliation

The owner-directed resolution preserves the dashboard truth without letting the unsupported control block unrelated work. `config/mrx-searchatlas-otto-vendor-exceptions.json` identifies only project `e4bab8bb-717e-480c-8dea-1de1b8596eb7`, audit `138239`, and record `182837670` with its exact verified fields. `pnpm check:searchatlas:otto-reconciliation` reports the item separately as vendor-locked and undeployed while requiring zero actionable pending records. Any changed status, deployment state, validity, page assignment, project/audit identity, inventory-count mismatch, or additional pending record fails the gate.

This is not a claim that the Search Atlas dashboard is zero. The accurate release accounting is **0 actionable pending + 1 vendor-locked undeployed suggestion**. `pnpm verify:schema:live-reconciliation` independently fails unless apex, `www`, and the native Vercel alias each return HTTP 200, expose exactly one authoritative `https://mineralrightsxchange.com/#org` Organization node, contain no Search Atlas/OTTO runtime injector, and contain no overlay ImageObject nodes on the tested homepage. The configured exception becomes unnecessary and safely disappears from the result if Search Atlas eventually removes the suggestion.

Implementation verification at `2026-09-15T03:58:16.913Z` passed on `https://mineralrightsxchange.com/`, the `www` redirect to apex, and `https://mrx-web.vercel.app/`: every response ended at HTTP 200 with exactly one Organization node, exactly one matching native `#org` entity, zero Search Atlas/OTTO runtime injector scripts, and zero homepage ImageObject overlay nodes. The focused reconciliation suite passes 9/9 cases, the full unit suite passes 80 files / 663 tests, lint and typecheck pass, and the full Vercel production build passes all existing MRX release, compliance, article-image, rendered-copy, SEO, and grammar gates.

## Human-support continuation and integrated release gate

Human representative Firman subsequently joined the same official Search Atlas conversation after the Voodoo handoff. Firman confirmed that leaving suggestion `182837670` undeployed means it does not reach the site and is not crawled. Firman declined a support escalation and directed the requested dismiss/satisfied capability to the Search Atlas Product Portal. Firman characterized the record as neither a product limitation nor a false positive; that label conflicts with the missing supported control confirmed by Jay, so this record preserves the narrower observable facts instead of assigning either disputed classification. Firman requested a screen recording to investigate the separate historical overlay recurrence. The overlay is not currently reproducible: fresh public verification shows no Search Atlas runtime injector and no overlay ImageObject nodes, so no misleading recording of a nonexistent current failure was created or transmitted.

Crawler-notification source through `5012401` was integrated without changing the exact vendor-exception boundary. Merge commit `3dd6bae` and the Node-24 workflow preservation commit `c43d208` are pushed on `release/mrx-measurement-ai-technical-20260909`. After integration, the full unit suite passes **80 files / 663 tests**, crawler tests pass **17 Python + 5 Node**, lint and typecheck pass, and the full Vercel build passes the MRX release gates, **230/230** evidence packets, **238** two-image article renders / **476** image binaries, **379** rendered SEO pages with zero sitemap orphans, copy, compliance, authorship, and grammar checks. Fresh live schema verification at `2026-09-15T04:11:02.232Z` passes apex, `www`, and `mrx-web.vercel.app` with HTTP 200, one authoritative native `#org` Organization entity, zero Search Atlas/OTTO runtime injector scripts, and zero homepage ImageObject overlay nodes.
